const fs=require("fs"),path=require("path"); let bad=[]; function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.name==='index.html'){const h=fs.readFileSync(p,'utf8');if(!/http-equiv="refresh"/.test(h)&&(!/<title>[^<]+<\/title>/.test(h)||!/<meta name="description"/.test(h)))bad.push(p)}}} walk(path.join(__dirname,"..")); if(bad.length){console.error(bad.join("\n"));process.exit(1)} console.log("PASS: title and description present on every indexable HTML page");
